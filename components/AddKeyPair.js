import React from 'react';
import {View, Text, StyleSheet, TouchableOpacity} from 'react-native';

const AddKeyPair = ({title}) => {
  return <View />;
};

const styles = StyleSheet.create({});

export default AddKeyPair;
